# ── Python / 工具缓存 ──
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/

# ── 打包产物 ──
dist/
build/

# ── 本地运行数据（禁止提交）──
# data/ 是插件运行时数据目录，含导出的 txt/json、已下载歌曲、登录 session 等
data/
store.db

# ── 密钥与登录态（禁止提交）──
# 网易云 / B站 / QQ音乐 的 cookie、session 会落在这类文件里
*.cookie.json
*.session.json
.env
.env.*
!.env.example

# ── 系统 / 编辑器 ──
.DS_Store
Thumbs.db
.vscode/
.idea/
